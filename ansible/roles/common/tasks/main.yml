---
- name: Mise à jour du cache apt
  apt:
    update_cache: yes
    cache_valid_time: 3600

- name: Installation des outils de base
  apt:
    name:
      - curl
      - wget
      - git
      - htop
      - ufw
      - ca-certificates
      - gnupg
      - lsb-release
    state: present
