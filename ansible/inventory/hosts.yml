all:
  hosts:
    lab:
      # On provisionne la machine locale (Claude Code tourne dessus) :
      # connexion locale = aucun risque de lockout SSH pendant le durcissement.
      ansible_connection: local
